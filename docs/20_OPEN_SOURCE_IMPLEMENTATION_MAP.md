# Open-source Implementation Map (M7–M12)

Planning map only. None of these milestones is started by this document. Each integration must re-check the pinned source, license and provenance before code is copied, adapted, linked or distributed.

| Milestone | Foundation / pinned revision | Source focus | License | Integration strategy / Starroom adapter | Required regression |
|---|---|---|---|---|---|
| M7 OKLCh Color Mixer | darktable `release-5.6.0`, tag object `f89bf9231fb21db0a53b3c279ff164caef48cef8`, commit `3c17b2976793303c186a5f64e8c9635ecf8b15d3` | `src/iop/colorzones.c`, hue-zone selection and smooth band behavior | GPL-3.0-or-later | Study mature zone semantics; Starroom owns an OKLab/OKLCh adapter in `starroom-color`, typed parameters and hue-lock. Any direct adaptation must be marked and added to provenance. | identity, hue-wrap continuity, skin/neon/landscape, finite HDR, shared Preview/Export |
| M8 Color Grading | same pinned darktable source | `src/iop/colorbalancergb.c` | GPL-3.0-or-later | Adapt through `starroom-grading`; keep scene-linear/perceptual stages native and UI math-free. | neutral identity, shadows/midtones/highlights separation, skin hue, HDR finite, Golden `grading,color,portrait,neon` |
| M9 Detail | same pinned darktable source | `src/iop/sharpen.c`, `src/iop/denoiseprofile.c`, local-contrast/detail modules selected during milestone review | GPL-3.0-or-later | Mature CPU provider behind `starroom-detail`; do not substitute blur/unsharp prototypes for claimed production denoise. Profile data receives separate license/hash review. | flat-field noise, edge MTF/halo, high ISO, fine texture, portrait, extreme finite |
| M10 Lens Correction | Lensfun `v0.3.4`, commit `101c745e847a5de4a1e569a94368ce2027198598` | public correction API plus separately reviewed lens database | LGPL-3.0-or-later; database audited separately | `starroom-optics` owns lookup/result adapter; prefer dynamic-link/distribution-compatible packaging. Cache database by version/hash. | identity/no-profile typed state, distortion, CA, vignetting, lens/camera match, Preview/Export parity |
| M11 Geometry | darktable pinned source above | `src/iop/ashift.c` and proven projective transform/crop logic | GPL-3.0-or-later | Typed transform/crop model in `starroom-geometry`; direct ports isolated and marked. Renderer owns resampling application. | identity, lines/architecture, inverse/round-trip, bounds/crop, extreme finite, no source overwrite |
| M12 GPU | wgpu `v30.0.0`, tag object `10239959d28d4283d89fd2fb0b04475881ba3f4f8`, commit `bf3e5ff4ab45e2c150e0d6c70d01d25f5b126c1` | official `wgpu` API, WGSL validation, DX12 backend | MIT OR Apache-2.0 | `starroom-render` backend selected behind stable stage contracts; CPU remains acceptance oracle until parity passes. Pin Cargo versions/lockfile at implementation time. | CPU/GPU RMSE/max/DeltaE, NaN/Inf, cache invalidation, 24/45/60/100 MP timings, device-loss typed fallback |
| M13 Tile / Preview Pyramid | no new third-party image engine; reuse pinned wgpu resource contract | Starroom scheduler/cache policy over typed render-graph halo declarations | Starroom-original architecture; wgpu remains MIT OR Apache-2.0 | `starroom-render::scheduler` owns preview levels, tile identity, cancellation, LRU budgets and viewport priority. It neither replaces image math nor adds a browser fallback. | 24/45/60/100 MP deterministic plans, halo coverage, visible-first ordering, stale rejection, RAM/VRAM LRU, cached-frame identity |
| M14 Adjustment Layers | no external image engine; reuse validated Native tone/color/curve stages | non-destructive document order and scene-linear Normal compositing | Starroom-original architecture | `starroom-project` persists layer intent; `starroom-pipeline` evaluates only typed, finite Normal layers. React transports/edits state and never executes image math. | order, enable, opacity, malformed contract, sidecar validation, Preview/Export parity, portrait/night/HDR Golden selection |
| M15 Mask Tree | no external image engine; reuse wgpu R16Float resource contract | composable local-edit masks and precision-safe Normal layer weight | Starroom-original architecture; wgpu remains MIT OR Apache-2.0 | `starroom-project` serializes a tree while `starroom-pipeline` owns the finite CPU reference. Provider masks remain explicit until their runtime is integrated. | radial/linear/brush/luminance/color range, boolean algebra, invert, invalid/provider error, opacity, persistence, portrait/backlight/HDR mask Golden selection |
| M16 Portrait Detection | OpenCV Zoo YuNet commit `47534e27c9851bb1128ccc0102f1145e27f23f98`; yakhyo Face Parsing BiSeNet **ResNet18** commit `8a4729d95118d0e97c44185f9bdef3d6bfeaaf99`; `ort 2.0.0-rc.10` | local detector bbox/5-landmarks and parser soft logits | YuNet MIT; `ort` MIT OR Apache-2.0; BiSeNet code MIT but model/data **NON_COMMERCIAL_ONLY / review before public release** | Local-only ONNX adapter in `starroom-portrait`; source-image semantic rasters enter M15 through a compact PortraitSemantic leaf and native cache, never React pixel math | no-face/multi-face/edge/crop/ID/cache/soft-mask/skin-exclusion/serialization/mask algebra; real model check only on an explicitly provisioned local model directory |
| M17 Skin Retouch | reuse fixed M16 YuNet/BiSeNet semantics; multi-scale implementation is Starroom-owned | frequency-aware low/high separation, skin/protected soft masks and OKLCh-local corrections | same M16 model restrictions; Starroom code under repository license | `starroom-portrait` evaluates all pixels in the Native graph; React edits bounded parameters only | neutral identity, texture/edge/protected-feature preservation, light/dark skin, mixed light, finite extremes, Preview/Export parity |
| M18 Healing / Remove | no new third-party engine | deterministic patch sampling and frequency/tone adaptation | Starroom-original implementation | `starroom-heal` owns Clone/Heal; shared source-normalized brush grammar supplies masks and patches without Browser pixel math | clone/heal, auto/manual source, edge patches, feather/opacity/transform, unsupported inpaint, serialization and parity |
| M19 Semantic Advisor | no model or external inference service | deterministic image/portrait statistics and explainable bounded rules | Starroom-original implementation | `starroom-advisor` receives Native graph statistics and returns compact recommendation intent; Preview remains reversible | percentile/clip/WB/portrait fixtures, rule conflicts, bounds, deterministic ordering, preview/apply/ignore/dismiss |
| M20 Local AI Masks | BiRefNet official `v1` asset SHA `5600024376f572a557870a5eb0afb1e5961636bef4e1e22132025467d0f03333`; NVIDIA SegFormer-B0 revision `489d5cd81a0b59fab9b7ea758d3548ebe99677da`, local ONNX SHA `56d255beface9e9f82ab68a1292b8b03881aa45161dffe914b7fb9657133dc58`; reuse `ort 2.0.0-rc.10` | Subject/Background soft matte and ADE20K Sky class 2; Person/Skin/Hair reuse M16 | BiRefNet MIT; SegFormer weight **NON_COMMERCIAL_ONLY / review before distribution**; `ort` MIT OR Apache-2.0 | local-only Native providers, SHA verification, DirectML-requested coherent sessions with explicit CPU fallback, generated-mask metadata in projects and native raster cache only | registry/hash/error/cancellation, probability/refinement, M15 algebra, serialization, cache isolation and shared Preview/Export |
| M21 AI Denoise | NAFNet commit `2b4af71ebe098a92a75910c233a3965a3e93ede4`, official SIDD width-32 checkpoint SHA `89c70e808d1783b6c07911306e106aaf0d4f7f3da8c61078b99ff7f8929a26f4`, local deterministic ONNX SHA `0e522d6de607958c283c834e6459a37b2fccbf5c19223a289393b4745f0cb633`; reuse `ort 2.0.0-rc.10` | fixed 512 FP32 model, model-domain V1 and residual adjustment before creative graph | MIT repository; checkpoint/SIDD redistribution review before public binary release | local-only model, native DirectML/CPU provider, 512/64 raised-cosine tiling and separate inference/control caches | domain round-trip/HDR, tile coverage/priority/seams, noisy flat/color/skin/cancellation/error, shared Preview/Export |
| M22 Reference Match | no new third-party runtime; reuse validated OKLab/OKLCh and existing Native adjustment stages | perceptual/statistical analysis and bounded parameter fitting | Starroom-original workflow implementation | `starroom-reference` emits only existing Tone/WB/Curve/Mixer/Grading intent; React does not implement math | identity/exposure/color/underexposure/skin protection, monotonic/finite/deterministic/serialization |
| M23 Portable Looks | no new third-party runtime; reuse monotone Hermite curves and Native detail/grade stages | `.srlook` schema, semantic interpolation, grain and vignette | Starroom-original workflow implementation | strict portable schema and `starroom-look`; final effects execute in the shared graph | JSON round-trip/reject, endpoints/A-B/circular hue/curve, deterministic grain/HDR vignette, Preview/Export parity |

## Milestone startup checklist

1. Put the exact milestone and only its modules into `codex/CURRENT_TASK.md`.
2. Verify tag/commit and license against official upstream; update provenance before importing code or data.
3. Choose integrate, adapt/port or validated replacement explicitly.
4. Run the target's Level 1 command, related Golden tags, Level 2 acceptance, then the required Full Acceptance trigger.
5. Never silently substitute a missing provider/profile/model or move creative image math into React/TypeScript.
